## 1.3.1+mc26.2 Changelog

### ***GENERAL***
- Added `"homepage"`, `"sources"` and `"issues"` links to `fabric.mod.json`

### ***CONFIGURATION***
- Added config file `config\servertweaks\server.properties` specifically for features meant only for Dedicated Servers
- Config key `playerAbilities` is no longer marked as `[EXPERIMENTAL]`
- Renamed config key `allowMixEnchantments` to `enableEnchantMixing`
- Renamed config key `afkCommandCooldown` to `afkUsageCooldown`
- Renamed config key `enableHigherEnchantmentLevels` to `enableHigherEnchantLevels`
- Renamed config key `disableAnvilLimit` to `disableAnvilTooExpensive`
- Renamed config key `fasterRiptideCharge` to `enableFastRiptideCharge`
- Renamed config key `rightClickHarvest` to `enableRightClickHarvest`
- Renamed config key `anvilRepair` to `enableAnvilRepair`
- Renamed config key `noAiNameTags` to `enableNoAINameTags`
- Renamed config key `obfuscateInvisDeathMessages` to `enableInvisDeathMessageObfuscation`
- Renamed config key `playerAbilities` to `enablePlayerAbilities`

### ***ADDED***
**Fixes & Workarounds**
- Fixed issue [#51](https://github.com/ErdbeerbaerLP/DiscordIntegration/issues/51) of [Discord Integration](https://modrinth.com/plugin/dcintegration) disrespecting `discordChatMessage` setting in `Messages.toml`
  - This will be removed when and if ErdbeerbaerLP fixes it himself

### ***BUG FIXES/TECHNICAL CHANGES***
***Commands***
- Fixed `/smpperms` always saying SMP permission level of a player was changed to default

***Data***
- Changed tag locations `entity_type\bug_eater\*` to `entity_type\insectivore\*`
- Changed location and renamed tags:
  - `item\diets\carnivore` to `item\diet_carnivore`
  - `item\diets\vegetarian` to `item\diet_vegetarian`
  - `item\diets\saccharivore` to `item\diet_saccharivore`
  - `item\diets\insectivore` to `item\diet_insectivore`
  - `item\diets\modifier_golden_foods` to `item\diet_golden_food`

### ***DEV STUFF***
- Updated `net.justmili.corelibs` package layout
- Moved all core packages and classes (util, mixin, registries etc.) to `net.justmili.servertweaks.core` package
- Added `Text` util class to `net.justmili.corelibs`, basically being a smaller `Component`
- Renamed some vars in `Config` class, not a big deal
- Tags now use data generation for automation